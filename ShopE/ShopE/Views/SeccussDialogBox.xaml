<?xml version="1.0" encoding="utf-8" ?>
<ContentView
    x:Class="ShopE.Views.SeccussDialogBox"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:d="http://xamarin.com/schemas/2014/forms/design"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="clr-namespace:ShopE.Models;assembly=ShopE"
    Margin="20,0"
    VerticalOptions="Center"
    mc:Ignorable="d">
    <ContentView.Content>
        <Frame BackgroundColor="#444" CornerRadius="10">
            <StackLayout Orientation="Horizontal">
                <Label
                    x:Name="messageLabel"
                    FontFamily="{StaticResource Shabname}"
                    HorizontalOptions="Center"
                    HorizontalTextAlignment="Center"
                    Text="خرید شما با موفقیت ثبت شد خریدار با شما تماس حاصل می فرماید"
                    TextColor="White"
                    VerticalOptions="Center" />
                <Label
                    FontFamily="{StaticResource MaterialFontFamily}"
                    FontSize="28"
                    HorizontalOptions="Center"
                    Text="{x:Static models:MaterialIconFont.CheckCircle}"
                    TextColor="ForestGreen"
                    VerticalOptions="Center" />
            </StackLayout>
        </Frame>
    </ContentView.Content>
</ContentView>